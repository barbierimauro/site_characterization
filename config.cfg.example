# config.cfg — Configurazione sito CRNS
# Copia questo file in config.cfg e inserisci i tuoi valori.
# Formato: NOME = TIPO, valore, [err+, err-, unita, descrizione]
# Tipi: STRG, INT, DBLE, REAL, CPLX, TUPLE

# --- Token OpenCelliD (richiesto per analisi RF) ---
OPENCELLID_TOKEN = STRG, "inserisci_qui_il_tuo_token", "Token API OpenCelliD"
